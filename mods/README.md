# Place user mods here (each subdir needs mod.json).
# See docs/modding.md, docs/modding-workshop.md, and examples/plugins/hello_courier/
# After dropping a pack: POST /api/reload_defs or action `mod_reload`.
# The host never auto-downloads unsigned packs from the internet.
